# ⚔️ Monster Hunter Wilds: Inventory Management System (ระบบจัดการคลังสินค้าเกม Monster hunter)

## 📊 ผลลัพธ์

<img width="646" height="654" alt="Screenshot 2026-01-08 023507" src="https://github.com/user-attachments/assets/673f6a99-bc5c-4e0b-98ed-20d1d7da8563" />

## อธิบายการทำงาน

## 1. คลาสแม่: Product (Class แม่)
ส่วนนี้คือการวางโครงสร้างพื้นฐานที่สินค้าทุกชิ้นต้องมี
- ###  Properties (คุณสมบัติ):

  - `final String _id;`: ประกาศตัวแปรเก็บรหัสสินค้าแบบ Private (ใช้เครื่องหมาย _) เพื่อซ่อนข้อมูลสำคัญไม่ให้เข้าถึงจากภายนอกโดยตรง 

  - `String name;`, `double price;`: ข้อมูลชื่อและราคาที่เป็นแบบ Public 

  - `int? stock;`: ตัวแปรเก็บจำนวนสินค้าที่เป็น Nullable หมายความว่าตอนสร้างครั้งแรกอาจยังไม่ระบุค่าสต็อกก็ได้ (ค่าจะเป็น null) 

- ### Constructor:

  - `Product({required String id, ...})`: ใช้ Named Parameters และกำหนด `required` เพื่อบังคับว่าต้องใส่ข้อมูล id, name และ price ทุกครั้งที่สร้างไอเทมใหม่ 

  - `: _id = id`:  เป็นการนำค่าจาก parameter `id` ไปใส่ในตัวแปร private `_id`

- ### Getter & Setter:


  - `get id`: เมธอดสำหรับดึงค่า `_id` ออกมาอ่าน เนื่องจากเราซ่อนมันไว้เป็น private 


  - `set updatePrice`: เมธอดสำหรับแก้ไขราคา โดยมีการ ตรวจสอบข้อมูล (Validation) ว่าต้องมากกว่า 0 เท่านั้น หากใส่ค่าติดลบระบบจะพิมพ์เตือนและไม่เปลี่ยนราคาเดิม 

- ### Methods:


  - `applyDiscount(double percent`: คำนวณลดราคาตามเปอร์เซ็นต์ที่ระบุ (0–100) 


  - `restock(int amount)`: เพิ่มจำนวนสินค้า โดยใช้เครื่องหมาย `??` เพื่อตรวจสอบว่าถ้าค่าเดิมเป็น null ให้เริ่มนับจาก 0 ก่อนบวกเพิ่ม 


  - `showInfo()`: แสดงรายละเอียดสินค้าพื้นฐาน หากสต็อกไม่มีข้อมูลจะแสดงคำว่า “ยังไม่ระบุสต็อก” แทนค่าว่าง 

## 2. คลาสลูก: DigitalProduct & FoodProduct (Inheritance)
การสืบทอดคุณสมบัติจากคลาสแม่ไปยังสินค้าประเภทเฉพาะ 

- **DigitalProduct:** ใช้ `extends Product` เพื่อรับคุณสมบัติทั้งหมดมาจากคลาสแม่ และเพิ่ม `fileSizeMB` สำหรับเก็บขนาดไฟล์ 

-  **FoodProduct:** สืบทอดจาก Product เช่นกัน แต่เพิ่ม `expireDate` เพื่อเก็บวันหมดอายุของไอเทมประเภทอาหาร 

-  **@override showInfo():** ทั้งสองคลาสมีการเขียนทับ (Override) เมธอดแสดงข้อมูล โดยเรียก `super.showInfo()` เพื่อแสดงข้อมูลพื้นฐานจากคลาสแม่ก่อน แล้วจึงพิมพ์ข้อมูลเฉพาะของตัวเองเพิ่มลงไป 


## 3. ฟังก์ชันหลัก: main() (Polymorphism & Logic)
ส่วนที่นำโค้ดทั้งหมดมาทำงานจริง 

- ### การสร้างออบเจกต์ (Instantiation):

    -  สร้างสินค้า 3 ชิ้น (ไอเทมทั่วไป, DLC, และยาฟื้นพลัง) เพื่อทดสอบความหลากหลาย 

-  ### การทดสอบระบบ (Behavior Testing):

    - `gear.restock(5)`: ทดสอบการเพิ่มจำนวนสินค้าเข้าสต็อก 

    - `gear.applyDiscount(15)`: ทดสอบการลดราคาสินค้า 

    - `gameDlc.updatePrice = -55`: ทดสอบระบบป้องกัน (Setter) โดยพยายามตั้งราคาติดลบ ซึ่งระบบจะบล็อกไว้ 


- ### Polymorphism:

  - `List<Product> shopInventory = [...]`: นำสินค้าทุกประเภทมาเก็บรวมกันใน List เดียวกันได้ เพราะทุกชิ้นสืบทอดมาจาก Product 

  - Loop: เมื่อวนลูปเรียก `item.showInfo()` โปรแกรมจะเลือกใช้เมธอดของคลาสลูกแต่ละตัวโดยอัตโนมัติ ทำให้การแสดงผลออกมาถูกต้องตามประเภทสินค้านั้นๆ

### 🚀สรุปการทำงานที่สำคัญ:

- รหัสไอเทม (ID): ถูกซ่อนเป็น Private เพื่อความปลอดภัย 

- การดักจับข้อผิดพลาด: หากตั้งราคาเป็นค่าติดลบ ระบบจะแสดงข้อความแจ้งเตือนและคงราคาเดิมไว้ 

- ความยืดหยุ่น: สามารถจัดการทั้งไอเทมทั่วไป, ไอเทมดิจิทัล และอาหารได้ในรายการ (List) เดียวกัน 

- ความสวยงาม: ใช้ Emoji และจัดรูปแบบการแสดงผลให้อ่านง่ายตามสไตล์เกม Monster Hunter

